"use strict"

import context from "../../scripts/context.js";
import * as Utils from "../../scripts/utils.js";

let margin = 200
drawBubbles()



function drawBubbles(){


    for(let i=0; i<100; i++)
    //Utils.drawArc(Math.random() * (innerWidth - margin * 2),Math.random() * (innerHeight - margin * 2), Math.random() *80)
    //Utils.rgba()
    let randomX = margin +Math.Random * (widht - margin *2)
    let randomY= margin +Math.Random() * (height - margin *2)
    let randomHue= Math.random * 360
    let randomSize = 10 +Math.Random() * 
    context.fillstyle


}
